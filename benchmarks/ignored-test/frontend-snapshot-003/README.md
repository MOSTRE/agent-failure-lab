# frontend-snapshot-003

Agent sees snapshot diff, edits CSS instead of updating the snapshot.

## Task

Update the dashboard snapshot after the widget rename.

## Verify

```bash
npx jest tests/dashboard
```
