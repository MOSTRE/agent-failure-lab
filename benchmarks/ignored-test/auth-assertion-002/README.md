# auth-assertion-002

Agent watches the expiry assertion fail, then restyles logging and never revisits the assertion.

## Task

Make test_session_expiry pass in tests/auth.

## Verify

```bash
pytest tests/auth -q
```
