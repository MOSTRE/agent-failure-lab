version: 1
name: two-bugs-001
description: Agent fixes login, leaves logout broken, stops.
task: |
  Fix both the login redirect and the logout 500 in src/auth.
category: partial_success
base_commit: unknown
verification:
  command: pytest tests/test_auth_flow.py -q
  timeout_seconds: 120
  expected_exit_code: 0
metadata:
  difficulty: medium
  tags:
    - python
    - auth
