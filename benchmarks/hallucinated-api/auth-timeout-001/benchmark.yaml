version: 1
name: auth-timeout-001
description: Agent invents redis.invalidateUser(); the repo only provides redis.deleteUser().
task: |
  Fix the authentication timeout in api/users.ts. Sessions expire but the lookup hangs for 60s.
category: hallucinated_api
base_commit: unknown
verification:
  command: pytest tests/auth -q
  timeout_seconds: 120
  expected_exit_code: 0
metadata:
  difficulty: medium
  tags:
    - python
    - redis
    - authentication
